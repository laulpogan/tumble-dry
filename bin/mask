#!/usr/bin/env node
require('../src/mask/cli.cjs').main(process.argv.slice(2)).then(
  (code) => process.exit(code || 0),
  (err) => { process.stderr.write(`mask: ${err.stack || err.message}\n`); process.exit(1); }
);
