# Sample markdown fixture

This is a tiny markdown file used by `tests/format.test.cjs` to verify the
identity-loader path. It should round-trip through `lib/loader.cjs` without
modification.

- bullet one
- bullet two

```
code fence
```
